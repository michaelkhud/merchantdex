# Sample secrets file for Kamal deployments.
# Copy to `.kamal/secrets` and fill values from a password manager.
# Never commit the populated `.kamal/secrets` file.

# Example: fetch secrets via 1Password CLI
# SECRETS=$(kamal secrets fetch --adapter 1password --account your-account --from Vault/Item KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
# RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})

# Example: fetch secrets via Rails credentials
# KAMAL_REGISTRY_PASSWORD=$(rails credentials:fetch kamal.registry_password)

# Example: fetch secrets from environment variables
# KAMAL_REGISTRY_PASSWORD=${KAMAL_REGISTRY_PASSWORD:?missing}
# RAILS_MASTER_KEY=${RAILS_MASTER_KEY:?missing}

# Export secrets for Kamal deploy
# KAMAL_REGISTRY_PASSWORD=your_registry_token
# RAILS_MASTER_KEY=your_rails_master_key













