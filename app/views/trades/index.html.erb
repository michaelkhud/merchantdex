<% content_for :title, "Trades - MerchantDex" %>

<div class="min-h-screen bg-gradient-to-br from-slate-50 via-white to-orange-50">
  <div class="max-w-7xl mx-auto px-6 lg:px-8 py-12">
    <!-- Header -->
    <div class="flex items-center justify-between mb-8">
      <div>
        <h1 class="text-3xl font-bold text-slate-900 mb-2">Your Trades</h1>
        <p class="text-slate-600">View and manage all your P2P trades</p>
      </div>
      <%= link_to dashboard_path, class: "inline-flex items-center gap-2 text-slate-600 hover:text-slate-900" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
        </svg>
        Back to Dashboard
      <% end %>
    </div>

    <!-- Stats -->
    <div class="grid md:grid-cols-4 gap-4 mb-8">
      <div class="bg-white rounded-xl p-4 shadow-sm border border-slate-200">
        <p class="text-sm text-slate-500">Total Profit</p>
        <p class="text-xl font-bold text-green-600">$<%= number_with_precision(@total_profit, precision: 2, delimiter: ',') %></p>
      </div>
      <div class="bg-white rounded-xl p-4 shadow-sm border border-slate-200">
        <p class="text-sm text-slate-500">Completed Trades</p>
        <p class="text-xl font-bold text-slate-900"><%= @total_trades %></p>
      </div>
      <div class="bg-white rounded-xl p-4 shadow-sm border border-slate-200">
        <p class="text-sm text-slate-500">Unique Clients</p>
        <p class="text-xl font-bold text-slate-900"><%= @unique_clients %></p>
      </div>
      <div class="bg-white rounded-xl p-4 shadow-sm border border-slate-200">
        <p class="text-sm text-slate-500">Platforms</p>
        <p class="text-xl font-bold text-slate-900"><%= @platforms.count %></p>
      </div>
    </div>

    <!-- Filters -->
    <div class="bg-white rounded-xl p-4 shadow-sm border border-slate-200 mb-6">
      <div class="flex flex-wrap gap-4">
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-1">Platform</label>
          <select onchange="window.location.href = this.value" class="px-3 py-2 border border-slate-300 rounded-lg text-sm">
            <option value="<%= trades_path %>" <%= 'selected' if params[:platform].blank? %>>All Platforms</option>
            <% @platforms.each do |platform| %>
              <option value="<%= trades_path(platform: platform) %>" <%= 'selected' if params[:platform] == platform %>><%= platform.titleize %></option>
            <% end %>
          </select>
        </div>
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-1">Type</label>
          <select onchange="window.location.href = this.value" class="px-3 py-2 border border-slate-300 rounded-lg text-sm">
            <option value="<%= trades_path(params.permit(:platform, :status)) %>" <%= 'selected' if params[:trade_type].blank? %>>All Types</option>
            <option value="<%= trades_path(params.permit(:platform, :status).merge(trade_type: 'buy')) %>" <%= 'selected' if params[:trade_type] == 'buy' %>>Buy</option>
            <option value="<%= trades_path(params.permit(:platform, :status).merge(trade_type: 'sell')) %>" <%= 'selected' if params[:trade_type] == 'sell' %>>Sell</option>
          </select>
        </div>
        <div>
          <label class="block text-sm font-medium text-slate-700 mb-1">Status</label>
          <select onchange="window.location.href = this.value" class="px-3 py-2 border border-slate-300 rounded-lg text-sm">
            <option value="<%= trades_path(params.permit(:platform, :trade_type)) %>" <%= 'selected' if params[:status].blank? %>>All Status</option>
            <option value="<%= trades_path(params.permit(:platform, :trade_type).merge(status: 'COMPLETED')) %>" <%= 'selected' if params[:status] == 'COMPLETED' %>>Completed</option>
            <option value="<%= trades_path(params.permit(:platform, :trade_type).merge(status: 'CANCELLED')) %>" <%= 'selected' if params[:status] == 'CANCELLED' %>>Cancelled</option>
          </select>
        </div>
      </div>
    </div>

    <!-- Trades Table -->
    <div class="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden">
      <% if @trades.any? %>
        <div class="overflow-x-auto">
          <table class="w-full">
            <thead class="bg-slate-50 border-b border-slate-200">
              <tr>
                <th class="px-4 py-3 text-left text-xs font-semibold text-slate-600 uppercase tracking-wider">Date</th>
                <th class="px-4 py-3 text-left text-xs font-semibold text-slate-600 uppercase tracking-wider">Platform</th>
                <th class="px-4 py-3 text-left text-xs font-semibold text-slate-600 uppercase tracking-wider">Type</th>
                <th class="px-4 py-3 text-left text-xs font-semibold text-slate-600 uppercase tracking-wider">Crypto</th>
                <th class="px-4 py-3 text-left text-xs font-semibold text-slate-600 uppercase tracking-wider">Amount</th>
                <th class="px-4 py-3 text-left text-xs font-semibold text-slate-600 uppercase tracking-wider">Counterparty</th>
                <th class="px-4 py-3 text-left text-xs font-semibold text-slate-600 uppercase tracking-wider">Status</th>
                <th class="px-4 py-3 text-right text-xs font-semibold text-slate-600 uppercase tracking-wider">Profit</th>
                <th class="px-4 py-3 text-right text-xs font-semibold text-slate-600 uppercase tracking-wider">Actions</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-slate-200">
              <% @trades.each do |trade| %>
                <tr class="hover:bg-slate-50">
                  <td class="px-4 py-3 text-sm text-slate-900">
                    <%= trade.time_completed&.strftime("%b %d, %Y") || trade.time_created&.strftime("%b %d, %Y") %>
                  </td>
                  <td class="px-4 py-3">
                    <span class="px-2 py-1 text-xs font-medium rounded-full bg-blue-100 text-blue-800">
                      <%= trade.platform.titleize %>
                    </span>
                  </td>
                  <td class="px-4 py-3">
                    <span class="px-2 py-1 text-xs font-medium rounded-full <%= trade.trade_type == 'buy' ? 'bg-green-100 text-green-800' : 'bg-orange-100 text-orange-800' %>">
                      <%= trade.trade_type.upcase %>
                    </span>
                  </td>
                  <td class="px-4 py-3 text-sm text-slate-900">
                    <%= number_with_precision(trade.crypto_amount, precision: 2, delimiter: ',') %> <%= trade.cryptocurrency %>
                  </td>
                  <td class="px-4 py-3 text-sm text-slate-900">
                    $<%= number_with_precision(trade.local_currency_amount, precision: 2, delimiter: ',') %> <%= trade.local_currency %>
                  </td>
                  <td class="px-4 py-3 text-sm text-slate-600">
                    <%= trade.counterparty || '-' %>
                  </td>
                  <td class="px-4 py-3">
                    <span class="px-2 py-1 text-xs font-medium rounded-full <%= trade.status == 'COMPLETED' ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800' %>">
                      <%= trade.status %>
                    </span>
                  </td>
                  <td class="px-4 py-3 text-sm text-right font-medium <%= trade.profit >= 0 ? 'text-green-600' : 'text-red-600' %>">
                    <%= trade.profit >= 0 ? '+' : '' %>$<%= number_with_precision(trade.profit, precision: 2, delimiter: ',') %>
                  </td>
                  <td class="px-4 py-3 text-right">
                    <%= button_to trade_path(trade), method: :delete, data: { turbo_confirm: "Are you sure you want to delete this trade?" }, class: "text-red-600 hover:text-red-800 text-sm" do %>
                      Delete
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="p-12 text-center">
          <svg class="w-12 h-12 text-slate-300 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
          </svg>
          <h3 class="text-lg font-medium text-slate-900 mb-2">No trades found</h3>
          <p class="text-slate-600 mb-4">Import your trades from the dashboard to get started.</p>
          <%= link_to "Go to Dashboard", dashboard_path, class: "inline-flex items-center gap-2 bg-orange-500 hover:bg-orange-600 text-white px-4 py-2 rounded-lg font-medium transition-colors" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

